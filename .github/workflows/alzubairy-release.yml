name: Build Yemen Remote

on:
  workflow_dispatch:
  push:
    branches:
      - worktree-port-forward-shared-conn
    paths:
      - ".github/workflows/alzubairy-release.yml"
      - ".github/workflows/flutter-build.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "libs/base/**"
      - "src/**"
      - "flutter/android/**"
      - "flutter/windows/**"
      - "branding/**"

permissions:
  contents: write
  actions: read

concurrency:
  group: yemen-remote-release
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: yemen-remote-beta
      windows-android-only: true
